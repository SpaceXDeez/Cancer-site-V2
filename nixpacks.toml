[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install", "npm --prefix client install"]

[phases.build]
cmds = ["npm --prefix client run build"]

[start]
cmd = "node server/index.js"
